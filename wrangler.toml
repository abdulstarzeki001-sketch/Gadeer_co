name = "gadeer-co"
# Nitro reads this file while building and adds the generated Worker entrypoint,
# static-assets binding, compatibility date, and nodejs_compat flag. Keep this
# source config free of legacy Wrangler fields so they are not copied into the
# generated .output/server/wrangler.json.
